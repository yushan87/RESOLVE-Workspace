Enhancement Get_Bottom_Capability for Globally_Bounded_Stack_Template;
	uses String_Theory;

	Operation Get_Bottom(updates S : Stack; updates E : Entry);
		requires |#S| > 0;
		ensures #S = S o <E>;

end Get_Bottom_Capability;
